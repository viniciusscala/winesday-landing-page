import React, { ReactElement } from 'react';
import { Button } from './styles';

function ComponentBoilerplate(): ReactElement {
  return (
    <div>
      <header>
        Testee
      </header>
      <Button />
    </div>
  );
}

export default ComponentBoilerplate;
